version: '3.8'

services:
  sistema-riego:
    build: .
    container_name: sistema-riego-bogota
    environment:
      - INTERVALO_RIEGO=3600
      - DURACION_RIEGO=300
      - HUMEDAD_UMBRAL=30.0
    volumes:
      - ./app:/app
    restart: unless-stopped
